# 家庭扩展验证记录

日期：2026-09-20。基于已有V2工作副本，保留155项历史回归。

## 已执行

- Python 3.12.14环境离线全量回归：208项通过（原有155项 + 家庭扩展53项）。
- v3.0.1修复Windows测试兼容性：图片大小用例使用短名称，在执行时生成超限数据；仍检查超过10 MiB的输入被拒绝。修复后需在用户Windows环境再次运行验证。
- v3.0.2增加阿里云Qwen3.8 Max/Flash的非思考与JSON输出参数；208项回归通过，并用真实OpenAI SDK和本地HTTP替身检查4种模型/服务组合的请求序列化与参数隔离。未进行真实百炼调用，需用户运行图片接口检查。
- 新增照片接收、格式/大小校验、私有路径限制、确认前不写档案、跨成员隔离、消息防重、草稿过期、照片软删除/撤销。
- 视觉请求结构、EXIF去除、无效输出拒绝、接口未配置降级、识别期间新消息到达不覆盖新草稿。
- 自然语言照片描述解析入口、缺日期继续追问、手动信息格式路径。
- 档案日期/类别/关键词筛选、缺日期保留、CSV导出、日报只统计对应日期的有效记录。
- 7条早教摘录全部与项目PDF原文核对。
- 时区、跨日汇总、补发窗口、默认不发送、成功后重启不重发、同时运行两个调度线程只发送一次。
- 发送超时不盲目重试、明确失败退避、准备期间暂停阻止发送。
- Streamlit AppTest打开全部五个功能页；生产模式密码保护；配置保存；预览不发送。
- SDK消息资源下载与主动发送请求结构测试；飞书私聊图片→描述→确认→共用档案完整链路（外部接口替身）。

- 更新包安装预览、SHA-256校验、旧代码备份、密钥/档案/照片原字节保留、回退条件检查及损坏包拒绝均已验证。
- Compose YAML可解析，三个服务共用同一持久化目录，网页默认仅绑定服务器回环地址；这不等于容器已构建。

## 需要用户账号和服务器完成

- 真实视觉服务调用：`python verify_features.py --live-vision`（仅发送合成圆形图片）。
- 飞书权限发布、群/私聊图片事件实际送达、两位成员同时使用。
- 实际定时推送和重启后的状态核对、至少两个自然日的连续运行。
- Docker构建、持久化卷权限、服务器进程重启、HTTPS、备份与恢复。

本次环境没有可用的Docker守护进程或真实账号凭证，未代发飞书消息、未发送真实宝宝照片、未发布服务器。离线测试不能替代这些验收。视觉描述仍可能有误；日期、首次发生等信息需家长核对。
