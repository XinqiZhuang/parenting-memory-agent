# V2 验证记录

代码基线：GitHub `ce827fb`。验证平台为 Linux / Python 3.12.14。

已在独立虚拟环境安装项目原有 requirements.txt 的全部锁定依赖，包含 openai 3.5.0、pydantic 2.13.4、pytest 9.1.1、pypdf 6.18.1、scikit-learn 1.9.1、streamlit 1.63.0、lark-oapi 1.7.3。

最新离线自动测试：155 项，其中新版 tests_v2 108 项，历史接口及其他回归47项。测试使用临时数据和模拟模型响应，禁止实际API调用。

覆盖：六类增查改删、显式确认、编号歧义、失效待确认、错误结构/日期/字段、写入失败回滚、消息防重、跨用户上下文隔离、跨线程和跨进程写入、乐观冲突、审计/撤销、照片软删除、旧数据保留、网页三页面加载、网页会话隔离、飞书适配层授权与参数传递。

安装器在临时GitHub基线副本上验证：保留未提交代码的备份，保持.env、baby.json和照片不变；安装后的副本再次运行全套回归。尚未产生新版数据时可以恢复原代码。

用户于2026-09-20反馈：Windows / Python 3.14.7 下新版108项离线测试通过，运行配置检查通过。真实DeepSeek命令解析评测为21/22；上传报告显示“记录宝宝今天第一次坐飞机”被误分为DEVELOPMENT，应为MEMORY。这是该小样本的分类结果，不代表总体准确率。

据此明确解析提示中的生活经历、发育能力与游戏活动边界，补充6条不同表达的评测用例，保留原有22条。修改后离线147项仍通过；用户于2026-09-20反馈真实模型28条用例全部通过。新增用例与原有用例用于迭代回归，不属于独立留出集。

随后用户反馈飞书实际操作正常：使用明确标记的测试回忆，完成新增预览、确认、查询、修改预览、确认、日志、撤销预览、确认和查询恢复后的描述，并清理测试记录。该验收由用户在本机执行；此处不将可选的另一位成员查询视为已单独确认，也不据此声称已完成真实多人并发验收。

用户随后在Windows / Python 3.14.7运行全量测试，得到155项通过（9.95秒）。检查剩余改动后，保留旧版活动检索、分类提示、追问和回答长度控制的兼容改进，以及test_activity_answer_style.py与test_activity_query.py中的8项测试；在Linux完整合并版本中复现155项通过（3.01秒）。147到155的差异来自这8项历史接口回归，V2核心仍是108项；这些模块未替代当前V2聊天处理链路。

本次V2尚未完成专门验收：真实多人并发压力、医疗知识回答质量、公开云部署安全评估。

提供 `python -m evaluation.evaluate_v2_parser --live`，可在用户本机用28条合成请求进行真实解析评估；不可将离线模拟测试描述为“自然语言准确率100%”。
